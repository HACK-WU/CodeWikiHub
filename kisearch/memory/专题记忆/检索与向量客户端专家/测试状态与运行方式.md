---
groupPath: 专题记忆/检索与向量客户端专家
relation: 测试状态与运行方式
exportedAt: "2026-09-08T02:26:39.717Z"
---
2026-09-07 实测：6 个测试文件 / **83 例全绿、0 失败**。
- test/vector-cli-functions.test.ts 32 例 ✅（检索策略单次多 tag OR + topk 放大、per-tag 限流 + ki-search 优先、批量输入四类校验、strict 护栏三项）
- test/scope-doc.test.ts 14 例 ✅（doc list/delete/tag list 空库行为、--yes 确认与预览、notFound、KB 层 scope 并集、default scope 拒绝删除）
- test/relation-map.test.ts 9 例 ✅（多值 memoryIds 聚合、缓存命中同实例、mtime/TTL 失效、文件缺失损坏降级、scope 隔离、旧 keywords/isFullText 忽略、tags 透传）
- test/search-original.test.ts 6 例 ✅（fetchOriginal 取原文、KB 缺失降级向量文档、多 chunk deduplicated）
- test/vector-idle-race.test.ts 1 例 ✅（embedding 期间 idle 窗口不打断操作）
- test/cli-aliases.test.ts 21 例 ✅（REQ-11 短别名 -s/-q/-t/-g/-r/-i/-n、REQ-12 位置参数、REQ-10 超长警告）

与 2026-08-28 记录的「79 例 / 78 通过 + 1 过期失败」相比 +4，两个独立原因：① **计数漂移**（先于本次改动存在）：vector-cli-functions 28→32、relation-map 8→9；② **2026-09-07 删除 1 例过期用例**：cli-aliases 22→21（该例断言早已移除的 `ki scan-kb diff -h` 含 `-o, --output`；随 CLI 扁平化改造清理）。

⚠️ 注意：REQ-11 原规划的 `-o`(--output) 短别名**在代码里从未实现**（export 走手写 argv 解析，只认 --output 长选项），故 cli-aliases 无 -o 用例，不属覆盖率损失。

运行：`env -u NODE_OPTIONS -u BASH_ENV npx jiti test/<name>.test.ts`；需真实召回的用例依赖 SILICONFLOW_API_KEY。
非 node:test 辅助：test/vector-client-smoke.mjs（手工冒烟）、test/e2e/*（端到端网络，未纳入常规回归）。
覆盖缺口（建议补）：ensureVectorAvailable.fastFail、probeWithRetry 撞锁重试、vectorDeleteScope 无进展保护、vectorDelete 的 NOT_FOUND 语义、generateDocId tag 参与回归保护、runWithVectorSource 来源透传、vectorListScopes/vectorCountScope、ki doc scope 护栏。

相关文件重命名（2026-09-07）：`src/scan-kb.ts` → `src/import.ts`；`test/e2e/scan-kb-cli.e2e.network.mjs` → `test/e2e/import-cli.e2e.network.mjs`（npm script `test:e2e:scan-kb` → `test:e2e:import`）。